# 实体层 (Entity)

## Entity-First 原则

Sea-ORM 实体优先定义，然后同步数据库架构。这意味着：

1. 先在代码中定义实体结构
2. 应用启动时自动创建/更新数据库表结构

## 实体定义

### 基本结构

```rust
use chrono::NaiveDateTime;
use rust_decimal::Decimal;
use serde::{Deserialize, Serialize};
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel, Deserialize, Serialize)]
#[serde(rename_all = "camelCase")]
#[sea_orm(table_name = "customer")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: i64,

    pub name: String,
    pub category: CustomerCategory,
    pub phone: String,
    pub wechat: Option<String>,
    pub address: Option<String>,
    pub bank_account: Option<String>,
    pub remark: Option<String>,
    pub create_at: NaiveDateTime,
}
```

### 字段类型映射

| Rust 类型       | 数据库类型    | 说明             |
| --------------- | ------------- | ---------------- |
| `i64`           | INTEGER       | 主键、ID         |
| `Decimal`       | DECIMAL(19,4) | 金额             |
| `String`        | TEXT          | 文本             |
| `NaiveDateTime` | DATETIME      | 日期时间         |
| `Option<T>`     | NULLABLE      | 可空字段         |
| 自定义枚举      | TEXT          | 枚举存储为字符串 |

### 特殊字段标记

```rust
// 主键（禁用自增，使用序列号生成）
#[sea_orm(primary_key, auto_increment = false)]
pub id: i64,

// 精确数值类型
#[sea_orm(column_type = "Decimal(Some((19, 4)))")]
pub amount: Decimal,

// 可选字段
pub remark: Option<String>,
```

> **注意**：`attachment` 实体例外，使用 `auto_increment = true`。

## ActiveModel 使用

### 插入数据

```rust
use sea_orm::ActiveValue;

let new_record = ActiveModel {
    id: ActiveValue::Set(id),
    amount: ActiveValue::Set(amount),
    record_time: ActiveValue::Set(record_time),
    accounting_type: ActiveValue::Set(accounting_type),
    title: ActiveValue::Set(title),
    // ...
    create_at: ActiveValue::Set(Local::now().naive_local()),
};

let inserted = new_record.insert(&db).await?;
```

### 更新数据

```rust
// 从现有 Model 转换
let mut active_model: ActiveModel = record.into();

// 只更新特定字段
active_model.amount = ActiveValue::Set(new_amount);
active_model.title = ActiveValue::Set(new_title);

let updated = active_model.update(&db).await?;
```

### ActiveValue 状态

```rust
// Set: 设置值
ActiveValue::Set(value)

// NotSet: 不设置（插入时使用默认值）
ActiveValue::NotSet
```

## ActiveModelBehavior

实现默认值和行为：

```rust
impl ActiveModelBehavior for ActiveModel {
    fn new() -> Self {
        use chrono::Local;
        let now = Local::now().naive_local();

        Self {
            id: sea_orm::ActiveValue::NotSet,
            amount: sea_orm::ActiveValue::NotSet,
            record_time: sea_orm::ActiveValue::NotSet,
            // 设置默认值
            create_at: sea_orm::ActiveValue::Set(now),
            state: sea_orm::ActiveValue::Set(AccountingRecordState::PendingPosting),
            remark: sea_orm::ActiveValue::Set(None),
            // ...
        }
    }
}
```

## ID 生成

### 标准格式：YYYYMMDDNNNNN

大多数实体使用基于日期和序列号的 ID 生成策略：

```rust
impl Model {
    /// 生成唯一 ID，格式为 YYYYMMDDNNNNN
    pub async fn generate_id(db: &DatabaseConnection) -> Result<i64, Box<dyn std::error::Error>> {
        let now = Local::now();
        let date_str = now.format("%Y%m%d").to_string();
        let date_int = date_str.parse::<i32>().unwrap_or(20210101);

        // 获取下一个序列号（通过对应的 seq 表原子递增）
        let next_seq = super::customer_seq::Model::get_next_sequence(db, date_int).await?;

        let id_str = format!("{}{:05}", date_int, next_seq);
        Ok(id_str.parse::<i64>().unwrap())
    }
}
```

> **特殊情况**：`accounting_book` 使用 `(year as i64) * 10000 + seq` 格式（如 `20240001`），因为它按年度而非日期管理序列。

## 序列表设计

每个主实体都有对应的 `_seq` 表，用于原子 ID 生成：

### 常见序列表（复合主键）

```rust
#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel)]
#[sea_orm(table_name = "customer_seq")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub date_key: i32,  // YYYYMMDD

    #[sea_orm(primary_key)]
    pub seq: i32,       // 当前序列号
}
```

### chat_message_seq（单主键）

`chat_message_seq` 使用 `id: i64` 单主键，额外包含 `date_key: i32` 和 `seq: i32` 字段，以及 `created_at: NaiveDateTime`：

```rust
#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel)]
#[sea_orm(table_name = "chat_message_seq")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: i64,
    pub date_key: i32,     // YYYYMMDD
    pub seq: i32,           // 当天序列号
    pub created_at: NaiveDateTime,
}
```

### category_seq

```rust
#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel)]
#[sea_orm(table_name = "category_seq")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: i32,  // 日期 YYYYMMDD
    pub seq: i32, // 当日流水号
}
```

## 各实体详情

### AccountingRecord（记账记录）

```rust
pub struct Model {
    pub id: i64,
    pub amount: Decimal,
    pub record_time: NaiveDateTime,
    pub accounting_type: AccountingType,
    pub title: String,
    pub channel: AccountingChannel,
    pub remark: Option<String>,
    pub write_off_id: Option<i64>,
    pub create_at: NaiveDateTime,
    pub state: AccountingRecordState,
    pub book_id: Option<i64>,
    pub order_id: Option<i64>,  // 关联订单 ID
}
```

关联关系：`AccountingBook`（通过 `book_id`）。

### AccountingBook（账本）

```rust
pub struct Model {
    pub id: i64,
    pub title: String,
    pub description: Option<String>,
    pub create_at: NaiveDateTime,
    pub record_count: i32,       // 记录数量（缓存字段）
    pub icon: Option<String>,    // 账本图标
}
```

### Category（品类）

```rust
pub struct Model {
    pub id: i64,
    pub name: String,              // 品类名称，唯一
    pub sell_book_id: i64,         // 销售账本外键
    pub purchase_book_id: i64,     // 进货账本外键
    pub remark: Option<String>,
    pub create_at: NaiveDateTime,
}
```

关联关系：`AccountingBook`（通过 `sell_book_id`）、`AccountingBook2`（通过 `purchase_book_id`）。

### SectionSummary（对话节摘要）

```rust
pub struct Model {
    pub id: i64,
    pub session_id: i64,           // 关联会话 ID
    pub section_file: String,      // 对应的 JSONL 文件名
    pub title: Option<String>,     // 摘要标题
    pub summary: String,           // 摘要内容
    pub created_at: NaiveDateTime,
}
```

无关联关系。ID 生成复用 `chat_message_seq` 序列表。

### Customer（客户）

```rust
pub struct Model {
    pub id: i64,
    pub name: String,
    pub category: CustomerCategory,
    pub phone: String,                 // 必填
    pub wechat: Option<String>,
    pub address: Option<String>,
    pub bank_account: Option<String>,
    pub remark: Option<String>,
    pub create_at: NaiveDateTime,
}
```

### Product（商品）

```rust
pub struct Model {
    pub id: i64,
    pub name: String,
    pub category_id: Option<i64>,      // 品类外键，关联 category.id
    pub category: Option<String>,      // 品类名称（冗余字段）
    pub unit: String,
    pub default_sell_price: Option<Decimal>,
    pub default_purchase_price: Option<Decimal>,
    pub sku: Option<String>,
    pub keywords: Option<String>,
    pub remark: Option<String>,
    pub create_at: NaiveDateTime,
}
```

关联关系：`Category`（通过 `category_id`）。

### Order（订单）

```rust
pub struct Model {
    pub id: i64,
    pub order_no: String,              // 可读订单编号（如 #1, #2）
    pub order_type: OrderType,
    pub customer_id: Option<i64>,
    pub total_amount: Decimal,
    pub actual_amount: Decimal,
    pub sub_type: OrderSubType,        // 订单业务类型
    pub status: OrderStatus,
    pub channel: AccountingChannel,    // 支付/收款渠道
    pub remark: Option<String>,
    pub create_at: NaiveDateTime,
    pub settled_at: Option<NaiveDateTime>,  // 结账时间
}
```

### Attachment（附件）

```rust
pub struct Model {
    #[sea_orm(primary_key, auto_increment = true)]
    pub id: i64,
    pub master_id: i64,
    pub path: String,
    pub file_name: String,
    pub file_suffix: String,
    pub file_size: String,
    pub create_at: NaiveDateTime,
}
```

> **注意**：`attachment` 是唯一使用 `auto_increment = true` 的实体。

## 关联关系

### 定义关联

```rust
#[derive(Copy, Clone, Debug, EnumIter)]
pub enum Relation {
    AccountingBook,
}

impl RelationTrait for Relation {
    fn def(&self) -> RelationDef {
        match self {
            Self::AccountingBook => Entity::belongs_to(super::accounting_book::Entity)
                .from(Column::BookId)
                .to(super::accounting_book::Column::Id)
                .into(),
        }
    }
}
```

### 多关联示例（category）

```rust
#[derive(Copy, Clone, Debug, EnumIter)]
pub enum Relation {
    AccountingBook,
    AccountingBook2,
}

impl RelationTrait for Relation {
    fn def(&self) -> RelationDef {
        match self {
            Self::AccountingBook => Entity::belongs_to(super::accounting_book::Entity)
                .from(Column::SellBookId)
                .to(super::accounting_book::Column::Id)
                .into(),
            Self::AccountingBook2 => Entity::belongs_to(super::accounting_book::Entity)
                .from(Column::PurchaseBookId)
                .to(super::accounting_book::Column::Id)
                .into(),
        }
    }
}
```

### 无关联的实体

```rust
// 无关联时使用空枚举 + DeriveRelation
#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {}
```

## 实体注册

在 `entity/mod.rs` 中集中注册所有实体：

```rust
pub async fn with_install_entities(
    db: &sea_orm::DatabaseConnection
) -> Result<(), Box<dyn std::error::Error>> {
    db.get_schema_builder()
        .register(accounting_record::Entity)
        .register(accounting_record_seq::Entity)
        .register(accounting_book::Entity)
        .register(accounting_book_seq::Entity)
        .register(attachment::Entity)
        .register(category::Entity)
        .register(category_seq::Entity)
        .register(chat_session::Entity)
        .register(chat_message_seq::Entity)
        .register(customer::Entity)
        .register(customer_seq::Entity)
        .register(product::Entity)
        .register(product_seq::Entity)
        .register(order::Entity)
        .register(order_item::Entity)
        .register(order_seq::Entity)
        .register(section_summary::Entity)
        .sync(db)
        .await?;

    Ok(())
}
```

## 文件组织

每个实体一个文件：

```
entity/
├── mod.rs              # 模块声明和注册
├── prelude.rs          # 公共导出
├── accounting_record.rs
├── accounting_record_seq.rs
├── accounting_book.rs
├── accounting_book_seq.rs
├── attachment.rs
├── category.rs
├── category_seq.rs
├── chat_session.rs
├── chat_message_seq.rs
├── customer.rs
├── customer_seq.rs
├── product.rs
├── product_seq.rs
├── order.rs
├── order_item.rs
├── order_seq.rs
└── section_summary.rs
```
