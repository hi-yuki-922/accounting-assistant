# 前端架构概览

本项目采用分层架构模式，从前端到后端的完整调用链为：

```
用户界面 (React 19 + TypeScript)
    ↓ TanStack Router
页面组件 (src/pages/*)
    ↓ 自定义 Hooks (src/hooks/*)
API 层 (src/api/commands/*)
    ↓ Tauri IPC (tryCMD)
Rust 后端命令 (src-tauri/src/commands/*)
    ↓ Service 层
业务逻辑层
    ↓ Data Access Layer
数据库层 (SQLite via Sea-ORM)
```

### 目录结构

```
src/
├── main.tsx              # 入口：RouterProvider + routeTree
├── ai/                   # AI Agent 基建（详见 [[AI集成]]）
│   ├── agent.ts          # Agent 工厂（组装 prompt + model + tools）
│   ├── provider.ts       # 智谱 AI Provider 配置
│   ├── router.ts         # 会话 section 路由
│   ├── writer.ts         # JSONL 流式写入器
│   ├── types.ts          # 共享类型定义（ToolResult）
│   ├── prompts/          # Prompt 系统
│   │   ├── loader.ts     # Markdown prompt 文件加载器
│   │   ├── fragments.ts  # 动态指令片段生成
│   │   ├── shared/       # 通用指令（base/guardrails/domain-knowledge）
│   │   └── agents/       # Agent 角色指令（team-leader）
│   ├── tools/            # 业务工具（15 个，5 类）
│   │   ├── index.ts      # 工具注册中心
│   │   ├── system.ts     # 系统工具
│   │   ├── basic-data.ts # 基础数据查询
│   │   ├── order.ts      # 订单管理
│   │   ├── accounting.ts # 记账工具
│   │   ├── interaction.ts # 交互控制（确认/补字段）
│   │   └── field-map.ts  # 写入工具字段定义（UI 表单）
│   └── storage/          # 存储层
│       ├── types.ts      # Session/SectionSummary/JSONLMessage
│       ├── session-store.ts  # 会话元数据 CRUD
│       ├── section-store.ts  # JSONL section 读写
│       └── summary.ts    # LLM 摘要生成
├── api/                  # API 层 - Tauri IPC 命令封装
│   ├── index.ts          # 统一导出
│   ├── types/
│   │   └── index.ts      # APIError, PaginationParams, PaginatedResult
│   ├── shared/
│   │   └── types.ts      # 跨模块共用类型（如 PaginatedResponse）
│   └── commands/         # 按模块组织的命令
│       ├── index.ts      # 统一导出所有命令模块和便捷对象
│       ├── accounting/
│       │   ├── index.ts
│       │   ├── enums.ts
│       │   └── type.ts
│       ├── accounting-book/
│       ├── attachment/
│       ├── category/     # 商品分类
│       ├── chat/
│       ├── customer/
│       ├── order/
│       └── product/
├── components/           # 通用组件
│   ├── ui/              # shadcn/ui 基础组件（~55）
│   ├── ai-elements/     # AI 聊天相关组件（~48）
│   ├── layouts/         # 布局组件
│   │   ├── app-layout.tsx
│   │   └── bottom-nav.tsx
│   └── theme-provider.tsx
├── config/              # 配置常量
│   └── book-icons.ts
├── hooks/               # 自定义 React Hooks
│   ├── use-section-chat.ts   # AI Section 对话管理（流式 + Agent 循环）
│   ├── use-section-list.ts   # Section 列表管理
│   ├── use-session-list.ts   # 会话列表管理
│   └── use-mobile.ts         # 移动端检测
├── lib/                 # 工具函数
│   ├── index.ts         # tryResult, tryResultAsync, parseJson, tryCMD
│   ├── utils.ts         # cn() 工具函数
│   ├── confirmation-mode.ts  # 确认模式管理
│   ├── message-utils.ts # 消息处理工具
│   ├── formatters.ts    # 格式化函数
│   ├── constants.ts     # 导航菜单、财务分类、图表颜色等
│   └── mock-data.ts     # 仪表盘模拟数据
├── pages/               # 页面组件
│   ├── dashboard/       # 仪表盘
│   │   └── components/
│   ├── books/           # 账本管理
│   │   ├── books-page.tsx
│   │   ├── book-detail-page.tsx
│   │   └── components/
│   ├── categories/      # 商品分类管理
│   │   ├── categories-page.tsx
│   │   └── components/
│   ├── chatbot/         # AI 聊天
│   │   ├── chatbot-page.tsx
│   │   └── components/  # 含 generative/ 生成式 UI 子目录
│   ├── customers/       # 客户管理
│   │   ├── customers-page.tsx
│   │   └── components/
│   ├── products/        # 商品管理
│   │   ├── products-page.tsx
│   │   └── *.tsx
│   └── orders/          # 订单管理
│       ├── orders-page.tsx
│       └── *.tsx
├── routes/              # TanStack Router 路由配置（文件路由）
├── styles/              # 全局样式
│   └── tailwind.css     # Tailwind v4 主题配置
└── types/               # TypeScript 类型定义
    ├── lib.ts           # SafeAsync, Safe, TryCMD
    ├── chatbot.ts       # 聊天相关类型
    ├── finance.ts       # 财务相关类型
    └── dashboard.ts     # 仪表盘类型
```
