# 前端架构概览

本项目采用分层架构模式，从前端到后端的完整调用链为：

```
用户界面 (React 19 + TypeScript)
    ↓ TanStack Router
页面组件 (src/pages/*)
    ↓ 自定义 Hooks (src/hooks/*)
API 层 (src/api/commands/*)
    ↓ Tauri IPC (tryCMD)
Rust 后端命令 (src-tauri/src/commands/*)
    ↓ Service 层
业务逻辑层
    ↓ Data Access Layer
数据库层 (SQLite via Sea-ORM)
```

### 目录结构

```
src/
├── main.tsx              # 入口：RouterProvider + routeTree
├── api/                  # API 层 - Tauri IPC 命令封装
│   ├── index.ts          # 统一导出
│   ├── types/
│   │   └── index.ts      # APIError, PaginationParams, PaginatedResult
│   ├── shared/
│   │   └── types.ts      # 跨模块共用类型（如 PaginatedResponse）
│   └── commands/         # 按模块组织的命令
│       ├── index.ts      # 统一导出所有命令模块和便捷对象
│       ├── accounting/
│       │   ├── index.ts
│       │   ├── enums.ts
│       │   └── type.ts
│       ├── accounting-book/
│       ├── attachment/
│       ├── chat/
│       ├── customer/
│       ├── order/
│       └── product/
├── components/           # 通用组件
│   ├── ui/              # shadcn/ui 基础组件（~50+）
│   ├── ai-elements/     # AI 聊天相关组件（~50+）
│   ├── layouts/         # 布局组件
│   │   ├── app-layout.tsx
│   │   └── bottom-nav.tsx
│   └── theme-provider.tsx
├── config/              # 配置常量
│   └── book-icons.ts
├── hooks/               # 自定义 React Hooks
│   ├── use-messages.ts
│   ├── use-sessions.ts
│   └── use-mobile.ts
├── lib/                 # 工具函数
│   ├── index.ts         # tryResult, tryResultAsync, parseJson, tryCMD
│   ├── utils.ts         # cn() 工具函数
│   ├── ai-provider.ts   # Zhipu AI provider（详见 [[AI集成]]）
│   ├── chat-tools.ts    # AI SDK 工具定义
│   ├── agent.ts         # Agent 包装
│   ├── formatters.ts    # 格式化函数
│   ├── constants.ts     # 导航菜单、财务分类、图表颜色等
│   └── mock-data.ts     # 仪表盘模拟数据
├── pages/               # 页面组件
│   ├── dashboard/       # 仪表盘
│   │   └── components/
│   ├── books/           # 账本管理
│   │   ├── books-page.tsx
│   │   ├── book-detail-page.tsx
│   │   └── components/
│   ├── chatbot/         # AI 聊天
│   │   ├── chatbot-page.tsx
│   │   └── components/
│   ├── customers/       # 客户管理
│   │   ├── customers-page.tsx
│   │   └── components/
│   ├── products/        # 商品管理
│   │   ├── products-page.tsx
│   │   └── *.tsx
│   └── orders/          # 订单管理
│       ├── orders-page.tsx
│       └── *.tsx
├── routes/              # TanStack Router 路由配置（文件路由）
├── styles/              # 全局样式
│   └── tailwind.css     # Tailwind v4 主题配置
└── types/               # TypeScript 类型定义
    ├── lib.ts           # SafeAsync, Safe, TryCMD
    ├── chat.ts
    ├── finance.ts
    ├── dashboard.ts
    └── agent.ts
```
